import './index.js'
import {
  allCustomers,
  singleCustomer,
  allRooms,
  allBookings,
  addNewBooking
} from './api'

console.log('allCustomers', allCustomers)
console.log('penis')

const randomNumber = () => Math.floor(Math.random() * 50)

randomNumber()
